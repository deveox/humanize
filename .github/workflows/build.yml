name: "Build"

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/actions/install-dependencies
      - run: npm test
      - run: npm run build
