name: "Build"

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ./github/workflows/install-dependencies.yml
      - run: npm test
      - run: npm run build
